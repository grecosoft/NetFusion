namespace NetFusion.Messaging
{
    /// <summary>
    /// Identifies a type as a message that is used to notify
    /// consumers that and event has happened.
    /// </summary>
    public interface IDomainEvent : IMessage
    {

    }
}
